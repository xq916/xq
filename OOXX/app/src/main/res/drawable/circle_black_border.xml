<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"> <!-- oval 形状用于圆形 -->
    <solid android:color="@android:color/transparent" /> <!-- 图片区域透明 -->
    <stroke
        android:width="2dp"
        android:color="#FF000000" /> <!-- 边框颜色（黑色） -->
    <size
        android:width="70dp"
        android:height="70dp"/>
</shape>